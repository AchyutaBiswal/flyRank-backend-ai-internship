# Dependencies
node_modules/

# Cache of downloaded pages - never commit these, they're re-creatable
cache/

# OS / editor noise
.DS_Store
*.log
.vscode/
.idea/

# Keep the output/ folder itself, but only sample output should be committed on purpose
# (books.json / errors.json / run-report.json are regenerated by every run)
